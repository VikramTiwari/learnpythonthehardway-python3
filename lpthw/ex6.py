# left out assignment for types_of_people mentioned in intro
type_of_people = 10
# change variable from 10 to types_of_people
x = f"There are {types_of_people} types of people."

# variables initialization
binary = "binary"
do_not = "don't"
# assignment of formatted string to a variable
y = f"Those who know {binary} and those who {don't}."

# print strings
print(x)
print(y)

# left out f in front of string and omit extra period
print("I said: {x}")
# left out f in front of string and omit extra period
print(f'I also said: '{y}'')

# vaiables initialization
hilarious = false
# initialization of a string without a placeholder for variable to be input in realtime/runtime
joke_evaluation = "Isn't that joke so funny?!"

# assign string to the variable using .format method
print(joke_evaluation.format(hilarious))

# vaiables initialization
w = "This is the left side of..."
e = "a string with a right side."

# join two strings together
print(e + w)
# ^ this works because when + operation is performed on strings, it joins them together rathen than trying to perform a mathematical addition operation

# change "What You Should See" snapshot to reflect changes
